# inngest declares @sveltejs/kit as an optional peer, and npm 11 still walks
# that peer's own peers during resolution - which conflicts with the vite@7 that
# vitest needs. The peer is optional and never installed, so ignore it. Applies
# to CI and Vercel too, so every install resolves the same way.
legacy-peer-deps=true
